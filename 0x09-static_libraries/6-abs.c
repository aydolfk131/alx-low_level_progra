#include "main.h"
/**
 * _abs - A function that checks an absolute value 
 * @n: integeral value
 * Return: the absolute value of the char
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
