#include "main.h"
#include <stdio.h>
/**
 * main - A function that prints its name
 * @argc: the count
 * @argv: the array of the count
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
